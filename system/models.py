from __future__ import unicode_literals
from django.db import models

"""
# Create your models here.
class Permissions(models.Model):
    class Meta:
        managed = False
        permissions = (
            ('change_config', 'Can change system config'),
            ('manage_daemons', 'Can manage daemons'),
            ('view_messages', 'Can view system messages'),
            ('clear_messages', 'Can clear system messages'),
        )
"""
