#
# Bacula Configuration file for Bacula Enterprise 8
# Configuration generated by ibadconf script
#
#  (c) 2017 by Inteos Sp. z o.o.
#
# .component: {{ Dir.compid_id }} : {{ Dir.compid.name }} : {{ Dir.compid.type }}

# resid: {{ Dir.resid }}
Director {
  Name = "{{ Dir.name }}"
  Password = "{{ Dirparams.Password }}"
}

# resid: {{ FD.resid }}
FileDaemon {
  Name = "{{ FD.name }}"
  Description = "{{ FD.description }}"
  MaximumConcurrentJobs = {{ FDparams.MaximumConcurrentJobs }}
  PidDirectory = "{{ FDparams.PidDirectory }}"
  PluginDirectory = "{{ FDparams.PluginDirectory }}"
  WorkingDirectory = "{{ FDparams.WorkingDirectory }}"
  HeartbeatInterval = {{ FDparams.HeartbeatInterval }}
  FDAddress = {{ FDparams.FDAddress }}
  FDPort = {{ FDparams.FDPort }}
}

# resid: {{ Mess.resid }}
Messages {
  Name = "{{ Mess.name }}"
  Description = "{{ Mess.description }}"
  Director = {{ Messparams.Director }}
}

# .end config: {{ Dir.compid_id }} : {{ Dir.compid.name }} : {{ Dir.compid.type }}
